<h1>Listing weights</h1>

<table>
  <tr>
    <th>User</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @weights.each do |weight| %>
  <tr>
    <td><%= weight.user_id %></td>
    <td><%= weight.value_string %></td>
    <td><%= link_to 'Show', user_weight_path(@user, weight) %></td>
    <td><%= link_to 'Edit', edit_user_weight_path(@user, weight) %></td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Weight', new_user_weight_path(@user) %>
