<%= form_for(@my_group_comment) do |f| %>
  <% if @my_group_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_group_comment.errors.count, "error") %> prohibited this my_group_comment from being saved:</h2>

      <ul>
      <% @my_group_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :group_member_id %><br />
    <%= f.text_field :group_member_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
