<script src="http://cdn.jquerytools.org/1.2.5/full/jquery.tools.min.js"></script>




<style>

  /* the overlayed element */
.apple_overlay {

	/* initially overlay is hidden */
	display:none;

	/* growing background image */
	background-image:url(<%= asset_path "overlay/white_large.png" %>);

	/*
		width after the growing animation finishes
		height is automatically calculated
	*/
	width:560px;

	/* some padding to layout nested elements nicely  */
	padding:35px;

	/* a little styling */
	font-size:11px;
}

/* default close button positioned on upper right corner */
.apple_overlay .close {
	background-image:url(<%= asset_path "overlay/close.png" %>);
	position:absolute; right:5px; top:5px;
	cursor:pointer;
	height:35px;
	width:35px;
}

a:hover {
   color: #fff;
   background-color:transparent;
 
}

/* black version of the overlay. simply uses a different background image */
div.apple_overlay.black {
background-image:url(<%= asset_path "overlay/transparent.png" %>);
color:#fff;
}

div.apple_overlay h2 {
margin:10px 0 -9px 0;
font-weight:bold;
font-size:14px;
}

div.black h2 {
color:#fff;
}

#triggers {
margin-top:10px;
text-align:center;
}

#triggers img {
background-color:#fff;
padding:2px;
border:1px solid #ccc;
margin:2px 5px;
cursor:pointer;
-moz-border-radius:4px;
-webkit-border-radius:4px;
}
</style>



<!--[if lt IE 7]>
<style>
div.apple_overlay {
background-image:url(http://static.flowplayer.org/tools/img/overlay/overlay_IE6.gif);
color:#fff;
}

/* default close button positioned on upper right corner */
div.apple_overlay div.close {
background-image:url(http://static.flowplayer.org/tools/img/overlay/overlay_close_IE6.gif);

}
</style>
<![endif]-->